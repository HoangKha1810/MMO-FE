import AiArenaRouteClient from './route-client';

export const dynamic = 'force-dynamic';
export const revalidate = 0;

export default function AiIntegratedCatchAllPage() {
  return <AiArenaRouteClient />;
}
