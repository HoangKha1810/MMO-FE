import { PageLoader } from '@/components/ui/page-loader';

export default function Loading() {
  return <PageLoader title="Đang tải không gian dịch vụ" subtitle="TRUNGTAMMMO đang chuẩn bị giao diện và đồng bộ dữ liệu mới nhất cho bạn." />;
}
