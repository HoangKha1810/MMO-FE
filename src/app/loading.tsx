import { PageLoader } from '@/components/ui/page-loader';

export default function Loading() {
  return <PageLoader title="Đang dựng giao diện" subtitle="Trang đang được render lại với theme, layout và dữ liệu mới nhất." />;
}
